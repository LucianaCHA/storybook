- luego de iniciar proyecto (este inicio con Vue que es lo mas reocmendable), en est caso con reaty + ts
- Paa iniciar story book npx storybook@latest init (esto analiza proyecto y sugiere deps si hacen falta)
---> │                                                                              │
│   Storybook was successfully installed in your project! 🎉                   │
│   To run Storybook manually, run npm run storybook. CTRL+C to stop.          │
│                                                                              │
│   Wanna know more about Storybook? Check out https://storybook.js.org/       │
│   Having trouble or want to chat? Join us at https://discord.gg/storybook/   │
│                                                                            

CONFIG 
- como este proyecto ssolo para storynook y nunc va a levantar la app, cambio el script de inicio
"scripts": {
    "dev": "vite",// my-storybook
    "build": "tsc && vite build",
    "lint": "eslint . --ext ts,tsx --report-unused-disable-directives --max-warnings 0",
    "preview": "vite preview",
    "storybook": "storybook dev -p 6006",
    "build-storybook": "storybook build"
  },


  7/7 


  Preparando el publis a npm 

  // elimino el build de vite  del package.json porque no quiero buildear un app en vite si no que quiero disponibiliza lo componentes como modulos . 